# Subagents

May request help from:

- `market-research` for audience language and proof.
- `analytics-experimentation` for keyword, conversion, and performance data.
- `growth-ops` for landing page and channel constraints.
- `lifecycle-crm` for lifecycle content reuse.

Return work in draftable formats with clear review requirements.
