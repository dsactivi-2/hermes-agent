# Subagents

May request help from:

- `content-strategy` for page copy and campaign assets.
- `analytics-experimentation` for tracking and funnel measurement.
- `lifecycle-crm` for lead routing and nurture.
- `market-research` for channel and competitor evidence.

Return executable plans with owners, dependencies, approvals, and metrics.
