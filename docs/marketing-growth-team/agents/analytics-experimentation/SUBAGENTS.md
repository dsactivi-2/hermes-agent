# Subagents

May request help from:

- `growth-ops` for funnel and launch context.
- `lifecycle-crm` for cohort and messaging definitions.
- `content-strategy` for content taxonomy.
- `market-research` for external benchmarks.

Return analysis with source, method, caveats, decision, and next action.
